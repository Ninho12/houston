import * as React from 'react';
import { ITableRow } from '../../interfaces';
interface IProps {
    collapse: unknown;
    row: ITableRow;
}
declare const _default: React.MemoExoticComponent<({ collapse, row }: IProps) => JSX.Element>;
export default _default;

import * as React from 'react';
import { IListItem } from '../../interfaces';
interface IProps {
    items?: IListItem[];
    stripedRows?: boolean;
}
declare const _default: React.MemoExoticComponent<({ items, stripedRows }: IProps) => JSX.Element>;
export default _default;
